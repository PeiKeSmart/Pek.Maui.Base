# JPush推送配置指南

## 问题描述
应用完全关闭后无法接收推送通知，但后台运行时可以正常接收。

## 解决方案

### 1. 代码配置（已完成）
- ✅ 添加了系统启动接收器
- ✅ 配置了前台保活服务
- ✅ 添加了电池优化忽略请求
- ✅ 增加了必要的权限和服务配置

### 2. 用户手动设置（需要用户操作）

#### 华为手机
1. 设置 → 应用 → 应用启动管理 → 找到你的应用 → 开启"自动管理"或手动开启"允许后台活动"
2. 设置 → 电池 → 启动管理 → 找到你的应用 → 设为"手动管理" → 开启"自启动"、"关联启动"、"后台活动"
3. 设置 → 通知 → 批量管理 → 找到你的应用 → 开启"允许通知"

#### 小米手机
1. 设置 → 应用设置 → 授权管理 → 自启动管理 → 找到你的应用 → 开启
2. 设置 → 电池与性能 → 神隐模式 → 找到你的应用 → 设为"无限制"
3. 设置 → 应用设置 → 应用管理 → 找到你的应用 → 电量优化 → 选择"无限制"

#### OPPO/OnePlus手机
1. 设置 → 电池 → 电池优化 → 找到你的应用 → 选择"不优化"
2. 设置 → 应用管理 → 找到你的应用 → 电池优化 → 允许后台运行
3. 手机管家 → 权限隐私 → 自启动管理 → 找到你的应用 → 开启

#### VIVO手机
1. 设置 → 电池 → 后台耗电管理 → 找到你的应用 → 选择"允许后台高耗电"
2. 设置 → 更多设置 → 权限管理 → 自启动 → 找到你的应用 → 开启
3. i管家 → 应用管理 → 自启动管理 → 找到你的应用 → 开启

#### 三星手机
1. 设置 → 设备保养 → 电池 → 应用电源管理 → 找到你的应用 → 设为"不优化"
2. 设置 → 应用程序 → 找到你的应用 → 电池 → 优化电池用量 → 关闭

#### 原生Android
1. 设置 → 电池 → 电池优化 → 找到你的应用 → 选择"不优化"
2. 设置 → 应用和通知 → 特殊应用权限 → 电池优化 → 找到你的应用 → 选择"不优化"

### 3. 测试方法

#### 测试步骤
1. 完全关闭应用（从最近任务中滑动删除）
2. 等待2-3分钟
3. 通过JPush控制台或其他方式发送推送
4. 检查是否能收到通知

#### 调试日志
查看Android Studio的Logcat，搜索以下标签：
- `JPushMessageReceiver`
- `JPushBootReceiver`
- `JPushKeepAliveService`

### 4. 常见问题

#### Q: 为什么后台可以收到，完全关闭就收不到？
A: Android系统为了省电，会限制应用在后台的活动。需要用户手动设置白名单。

#### Q: 配置后还是收不到怎么办？
A: 
1. 检查是否已授予通知权限
2. 检查是否已忽略电池优化
3. 检查厂商的自启动管理设置
4. 重启手机后再次测试

#### Q: 不同Android版本有区别吗？
A: 是的，Android 6.0+引入了电池优化，Android 8.0+对后台服务有更严格限制，Android 13+需要通知权限。

### 5. 最佳实践

1. **引导用户设置**：在应用首次启动时，引导用户进行必要的权限设置
2. **检测设置状态**：定期检查电池优化和自启动设置状态
3. **提供设置入口**：在应用设置页面提供快速跳转到系统设置的入口
4. **多重保障**：结合前台服务、系统广播接收器等多种方式确保推送可达

### 6. 注意事项

- 不同厂商的设置路径可能略有差异
- 部分设置需要用户手动操作，无法通过代码自动完成
- 建议在应用中添加设置指引页面，帮助用户完成配置
- 定期测试不同品牌手机的推送效果